#include<iostream>
using namespace std;

int LinearSearch(int arr[],int key)
{

    int n = arr.size();
    for(int i = 0;i < n;i++)
    {
        if(arr[i] == key)
        {
            if(key > 20)
            {
                return 
            }
        }
    }
}

int main(){
    
    return 0;
}