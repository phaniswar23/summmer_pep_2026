#include<iostream>
using namespace std;
class TreeNode
{
    int data;
    TreeNode* left;
    TreeNode* right;
    TreeNode(int val)
    {
        data = data;
        left = nullptr;
        right = nullptr;
    }
};
void preorder(TreeNode* root)
{
    //base case
    if(root == NULL) return;
    preorder
}
int main(){
    
    return 0;
}